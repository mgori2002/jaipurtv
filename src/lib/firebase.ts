// Firebase is no longer used in this project. This file will be removed once the
// new GitHub-backed authentication and content storage flow is implemented.
// Temporary stubs are exported to avoid breaking existing imports during the
// migration.

export const auth = null;
export const db = null;
export const analyticsPromise = Promise.resolve(null);
