{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/contact", "dest": "server/contact.ts" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
